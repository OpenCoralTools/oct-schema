# Excel Workbook Templates

Spreadsheet templates for working with validated coral data.

## Usage

1. Open `oct_schema.xlsx` in Excel or Google Sheets
2. Each sheet corresponds to a schema class
3. Column headers match schema properties
4. Use for data entry, import/export, or sharing with collaborators

## Validation

Data entered in these templates can be exported to JSON and validated against the JSON Schema.
