id: https://w3id.org/OpenCoralTools/oct-schema/classes/Coral
name: Coral
title: Coral Class
description: Represents a specific coral instance

prefixes:
  oct_schema: https://w3id.org/OpenCoralTools/oct-schema/
  linkml: https://w3id.org/linkml/

default_prefix: oct_schema
default_range: string

imports:
  - InventoryRecord

classes:
  Coral:
    is_a: InventoryRecord
    description: Represents a specific coral instance
    slots:
      - name
      - genetId
      - speciesId
      - siteId
      - groupId
      - coralTypeId
      - quantity
      - coral_size
    slot_usage:
      name:
        required: true
      genetId:
        required: true
      speciesId:
        required: true
      siteId:
        required: true
      groupId:
        required: true
      coralTypeId:
        required: true
      quantity:
        required: true

slots:
  genetId:
    range: string
    description: ID of the genet
  speciesId:
    range: string
    description: ID of the species
  groupId:
    range: string
    description: ID of the group/location
  coralTypeId:
    range: string
    description: ID of the coral type
  quantity:
    range: integer
    description: Quantity of items
  coral_size:
    range: CoralSize
    description: Size of the coral
